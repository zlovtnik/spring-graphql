# ssf

Spring Boot GraphQL API with SQLite, HTTPS, JWT, and MinIO integration.

## Features
- GraphQL API
- Local SQLite database
- HTTPS ready
- JWT authentication (to be configured)
- MinIO integration (to be configured)

## Getting Started
1. `./gradlew bootRun`
2. Configure `application.properties` for HTTPS, JWT, and MinIO as needed.

---

Generated with GitHub Copilot.
